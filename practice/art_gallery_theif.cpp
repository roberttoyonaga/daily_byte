#include <vector>
#include <iostream>
using namespace std;

int ArtGalleryThief(int maxWeight, vector<int>& weights, vector<int>& values)
{
    int maxVal = 0;

    for (int i = 0; i < weights.size(); i++)
    {
        
    }

    return maxVal;
}

int main()
{
    vector<int> weights{4, 1, 3};
    vector<int> values{4, 2, 7};
    cout<<ArtGalleryThief(10, weights, values);
}